# run.sh
> A terminal-based prank script that exposes your darkest secret.

## What is this?

`run.sh` is a harmless and hilarious Bash script that simulates a **fake system diagnostic**.

## Not working?

Use `chmod +x run.sh` in case of it not running. It appends permissions to run the file.
